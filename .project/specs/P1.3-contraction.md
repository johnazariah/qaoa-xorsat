# Spec P1.3 — QAOA Evaluation (Revised)

**Phase**: 1 (Mathematical Foundation)
**Status**: Revised — incorporates finite-D Basso structure and the WHT factorisation discovery
**Depends on**: P1.1 (Factor Graph Tree), P1.2 (Tensor Network)
**Blocks**: Phase 4 (Computation & Optimisation)

---

## Goal

Implement exact QAOA expectation value evaluation for Max-k-XORSAT on D-regular
hypergraphs at depth p. The implementation has **three tiers** with different
cost/reach tradeoffs.

## Critical Correction

The original P1.3 spec claimed O(4^p) cost for all k via entrywise exponentiation
at both variable and constraint nodes. **This is wrong for k > 2.**

At a constraint node, the k-body problem gate entangles all k children's
hyperindices. For k-1 > 1, the children's contributions combine **multilinearly**
through the k-body gate, not via entrywise power.

Source: Basso et al. 2021, §8, Eqs. (8.7)–(8.8). See
`.project/learning/12-transfer-contraction-k-body.md` for the full analysis.

## New Correction: WHT Eliminates the `4^{kp}` Fold Barrier

The previous revised spec still treated the exact finite-D constraint fold as an
`O(p · 4^{kp})` step because it summed explicitly over all child branch tuples.

That complexity estimate is now outdated. The new note
`.project/learning/15-wht-factorisation-discovery.md` shows that the k-body
constraint fold is a convolution on `(ℤ₂^{2p+1}, ⊕)` and can be computed via a
Walsh-Hadamard transform factorisation.

Consequences:

- exact finite-D contraction is now expected to cost `O(p · 4^p)` per fold step
- total exact finite-D cost is now expected to be `O(p² · 4^p)`
- this complexity is independent of `k` except for a pointwise power in the WHT domain

This resolves the earlier concern that exact finite-D `k=3` would top out near
`p ≈ 5–7` purely because of explicit child-tuple enumeration.

## Three-Tier Architecture

### Tier 1: Brute-Force Light-Cone Simulator (DONE)

**Cost**: O(2^n) where n = total_variables(k, D, p)
**Reach**: k=2 up to p≈4; k=3,D=4 up to p=1. At p=2 the light cone already
requires 129 qubits, so Tier 1 is infeasible under the current exact-reference
approach.
**Status**: Implemented in `src/qaoa.jl`, validated, 303 tests passing

This is the reference oracle. It explicitly builds the light-cone tree, simulates
the state vector, and measures the root observable. Provably correct by
construction. All other implementations must match it at overlapping p values.

### Tier 2: Exact Finite-D Iteration with WHT Fold (TO IMPLEMENT)

**Cost**: O(p² · 4^p)
**Reach**: exact finite-D work to roughly the same depth regime as compact MaxCut recursion, subject to constants and memory
**Source**: Basso et al. 2021, §8.2, Eq. (8.7), plus the WHT factorisation in `.project/learning/15-wht-factorisation-discovery.md`

This is the next implementation target. It uses the Basso iterative formula
with a Walsh-Hadamard accelerated exact constraint fold. The formula remains
exact at finite D — no approximations.

### Tier 3: Basso Large-D Iteration (FUTURE)

**Cost**: O(p² · 4^p) — independent of k
**Reach**: p ≤ 15+ easily
**Source**: Basso et al. 2021, §8.3, Eqs. (8.9)–(8.10)
**Caveat**: O(1/D) approximation. At D=4 the error is ~25%. Useful as
bound and for angle optimisation seeding, not for precise comparison.

## Tier 2 Design (Primary Implementation Target)

### The Basso Finite-D Iteration

The branch tensor $H_D^{(m)}(\mathbf{a})$ is a function from $(2p+1)$-bit strings
to reals. Starting with $H_D^{(0)} = 1$, each of p steps computes:

$$H_D^{(m)}(\mathbf{a}) = \left[\sum_{\mathbf{b}^1,\ldots,\mathbf{b}^{k-1}} \cos\!\left(\frac{1}{\sqrt{D}} \boldsymbol{\Gamma} \cdot (\mathbf{a}\,\mathbf{b}^1\cdots\mathbf{b}^{k-1})\right) \prod_{i=1}^{k-1} f(\mathbf{b}^i) H_D^{(m-1)}(\mathbf{b}^i)\right]^D$$

The final expectation (Eq. 8.8):

$$\langle Z_1 \cdots Z_k \rangle = -i \sum_{\mathbf{a}^1,\ldots,\mathbf{a}^k} \sin\!\left(\frac{1}{\sqrt{D}} \boldsymbol{\Gamma} \cdot (\mathbf{a}^1\cdots\mathbf{a}^k)\right) \prod_{j=1}^{k} a_j^{[0]} f(\mathbf{a}^j) H_D^{(p)}(\mathbf{a}^j)$$

And the satisfaction fraction: $c_\alpha = (1 - \langle Z_1 \cdots Z_k \rangle) / 2$

### Convention mapping: Basso → our codebase

| Basso | Ours | Notes |
|-------|------|-------|
| $q$ | `k` | Constraint arity |
| $D$ | `D - 1` | Basso D = branching hyperedges; our D = total degree |
| $\boldsymbol{\Gamma}$ | Derived from `QAOAAngles` | See Basso §3 |
| $f(\mathbf{a})$ | Product of mixer matrix elements | Related to P1.2 tensors |
| $H_D^{(m)}$ | `branch_tensor` at step m | Vector of $2^{2p+1}$ entries |

**CRITICAL**: Basso's D is our D - 1. He works with (D+1)-regular graphs where
each variable has D branching hyperedges. Verify by matching node counts.

### Cost verification

| p | 4^p state size | Per-step transform work | Memory |
|---|----------------|-------------------------|--------|
| 1 | 4 | trivial | tiny |
| 3 | 64 | modest | tiny |
| 5 | 1,024 | laptop-scale | small |
| 10 | 1,048,576 | large but realistic | MB-scale |
| 15 | 1,073,741,824 | heavy / cluster-scale | GB-scale |

The exact practical ceiling still depends on constants, transform reuse, and the
root observable implementation, but the asymptotic blocker is no longer the
naive `4^{kp}` child enumeration.

## Test Plan

### Clause-sign convention

The `clause_sign` parameter controls the parity convention:
- **MaxCut (k=2)**: `clause_sign = -1` — the cost operator is $(1 - Z_i Z_j)/2$
  (satisfied when spins differ, i.e. odd parity)
- **k-XORSAT (k≥3)**: `clause_sign = +1` — the cost operator is $(1 + Z_1 \cdots Z_k)/2$
  (satisfied when the XOR is even, with all $J = -1$ after gauge fixing)

The Basso formula (Eq. 8.5) fixes all couplings to $J = -1$ after gauge fixing,
so the observable is $-\langle Z_1 \cdots Z_k \rangle$ and the satisfaction fraction
is $(1 - \langle Z_1 \cdots Z_k \rangle)/2$. The `basso_expectation` function must
return this directly — the caller should NOT need to set `clause_sign`.

### Cross-validation with Tier 1 (critical — non-negotiable)

```julia
@testset "Basso vs brute-force" begin
    # MaxCut: clause_sign = -1 (odd parity / antiferromagnetic)
    @testset "MaxCut k=2, D=3, p=$p" for p in 1:3
        params = TreeParams(2, 3, p)
        angles = QAOAAngles(rand(p) .* π, rand(p) .* π)
        brute = qaoa_expectation(params, angles; clause_sign = -1)
        basso = basso_expectation(params, angles)
        @test brute ≈ basso atol=1e-10
    end

    # k-XORSAT: clause_sign = +1 (even parity / all J = -1 after gauge fix)
    @testset "XORSAT k=3, D=4, p=1" begin
        params = TreeParams(3, 4, 1)
        angles = QAOAAngles([0.3], [0.5])
        brute = qaoa_expectation(params, angles; clause_sign = 1)
        basso = basso_expectation(params, angles)
        @test brute ≈ basso atol=1e-10
    end
end
```

### MaxCut p=1 optimum

```julia
@testset "MaxCut p=1 optimum via Basso" begin
    params = TreeParams(2, 3, 1)
    angles = QAOAAngles([atan(1/sqrt(2))], [π/8])
    @test basso_expectation(params, angles) ≈ 0.5 + sqrt(3)/9 atol=1e-10
end
```

### Properties: zero-angle, boundedness, periodicity

```julia
@testset "Basso properties" begin
    @testset "zero angles → 0.5" for k in [2,3], D in [3,4], p in 1:2
        @test basso_expectation(TreeParams(k,D,p), QAOAAngles(zeros(p),zeros(p))) ≈ 0.5 atol=1e-10
    end

    @testset "bounded [0,1]" begin
        for _ in 1:100
            angles = QAOAAngles([rand()*2π], [rand()*2π])
            @test 0.0 ≤ basso_expectation(TreeParams(3,4,1), angles) ≤ 1.0
        end
    end
end
```

### Scaling benchmarks (instrumented, not hard-pass)

These benchmarks target the Tier 2 Basso iteration (`basso_expectation`), NOT the
Tier 1 brute-force evaluator (which is guarded to ≤22 qubits and cannot run
at these parameters).

```julia
# Tier 2 scaling — should show O(4^{3p}) growth
for p in 1:5
    params = TreeParams(3, 4, p)
    angles = QAOAAngles(rand(p), rand(p))
    @time basso_expectation(params, angles)
end
```

## Acceptance Criteria

1. Tier 2 matches Tier 1 to 1e-10 at all overlapping (k, D, p) values
2. MaxCut p=1 optimum ≈ 0.6924
3. Zero angles → 0.5 for all (k, D)
4. Results bounded in [0, 1]
5. Cost scales as O(p² · 4^p) — verified empirically
6. Memory is O(4^p) — child sums computed on the fly
7. Runs at k=3, D=4, p=5 within minutes on a laptop

## Implementation Order

0. **Review the Basso reference implementation** at
   `github.com/benjaminvillalonga/large-girth-maxcut-qaoa`. This is the upstream
   code for the compact MaxCut recursion. Check how they handle the Γ vector,
   f(a) function, and the iteration loop. Note: their code is MaxCut-only (k=2)
   but the structure translates directly to the k-XORSAT generalisation.

1. **Read Basso 2021 §8.2, §8.4.** Map every variable to our notation. Document
   the mapping in a comment block at the top of the source file.

2. **Implement helpers**: `build_gamma_vector`, `f_function`, `decode_bits`.

3. **Implement the exact branch/root conventions for k=2**. Validate against Tier 1 at p=1,2,3.

4. **Replace naive child-tuple summation with the WHT fold**.

5. **Validate k=3** against Tier 1 at p=1.

6. **Benchmark** p=1..5 at k=3, D=4.

7. **Run angle optimisation** at each feasible p and record results.

## Open Questions

1. **WHT production details**: how should the transformed kernel and branch
    tensor be cached across depth steps to minimise temporary allocations and
    repeated kernel builds?

2. **Angle optimisation cost**: At p=5, each function eval costs ~10^9 ops. With
   ~100 L-BFGS evals × ~10 restarts = ~10^{12} total. Feasible on a cluster.
   Warm-start from smaller-p solutions to reduce restarts.

3. **Whether p=5–7 is enough**: SA achieves 0.9366 at (k=3, D=4). The Basso
   large-D results (Appendix D) can estimate what QAOA achieves at each p.
   If the infinite-D numbers at p=5 already exceed 0.9366, exact finite-D
   confirmation is the goal. If they don't exceed it even at p=15, the answer
   is "QAOA doesn't beat SA here" and we report that.
- $q$ is our $k$ (constraint arity)
- $D$ is the number of branching hyperedges per variable (our $D-1$ in the non-root context)
- $\boldsymbol{\Gamma}$ encodes the QAOA angles
- $f(\mathbf{b})$ is a product of mixer matrix entries
- The sum over $\mathbf{b}^1, \ldots, \mathbf{b}^{q-1}$ ranges over all $(2p+1)$-bit strings for each of the $q-1$ child variables

## Key Observation: The Cost is $O(p \cdot 4^{pq})$, NOT $O(p \cdot 4^p)$

The sum over $\mathbf{b}^1, \ldots, \mathbf{b}^{q-1}$ has $4^{p(q-1)}$ terms (each $\mathbf{b}^i$ ranges over $4^p$ values). Combined with the $4^p$ entries of $\mathbf{a}$, each iteration step costs $O(4^{pq})$.

For $q = k = 2$ (MaxCut): cost is $O(4^{2p}) = O(16^p)$ per step... but the cosine factorises into a product of 2-body terms, which reduces to $O(4^p)$. This is the Farhi 2025 trick.

For $q = k = 3$ (our target): cost is $O(4^{3p})$ per step naively. At $p = 10$ that's $4^{30} \approx 10^{18}$ — **completely infeasible**.

## But Wait: The Large-D Limit Gives $O(p^2 \cdot 4^p)$

Basso 2021 Eq. (8.9)–(8.10) gives a compact iteration in the $D \to \infty$ limit:

$$G_{j,k}^{(m)} = \sum_{\mathbf{a}} f(\mathbf{a}) a_j a_k \exp\!\left(-\frac{1}{2} \sum_{j',k'} \left(G_{j',k'}^{(m-1)}\right)^{q-1} \Gamma_{j'} \Gamma_{k'} a_{j'} a_{k'}\right)$$

And the final result:

$$\nu_p^{[q]}(\gamma, \beta) = \frac{i}{\sqrt{2q}} \sum_j \Gamma_j \left(G_{0,j}^{(p)}\right)^q$$

Note the $q$-th power of the matrix element — that's the only place $q$ enters. This iteration has cost $O(p^2 \cdot 4^p)$ and memory $O(p^2)$, **regardless of q**!

But this is the **large-D limit** with $O(1/D)$ corrections — exactly the approximation we're trying to avoid at $D = 4$.

## The Middle Ground: Exact Finite-D at Moderate Cost?

The core question is whether the $O(4^{pq})$ cost at finite D can be reduced. Let me examine the structure of Eq. (8.7) more carefully.

The sum over $\mathbf{b}^1, \ldots, \mathbf{b}^{q-1}$ at fixed $\mathbf{a}$ involves:

$$\cos\!\left(\frac{1}{\sqrt{D}} \boldsymbol{\Gamma} \cdot (\mathbf{a}\,\mathbf{b}^1 \cdots \mathbf{b}^{q-1})\right) \prod_{i=1}^{q-1} f(\mathbf{b}^i) H_D^{(m-1)}(\mathbf{b}^i)$$

The cosine is a function of the **joint parity** of $\mathbf{a}, \mathbf{b}^1, \ldots, \mathbf{b}^{q-1}$ at each round position. The inner product $\boldsymbol{\Gamma} \cdot (\mathbf{a}\,\mathbf{b}^1 \cdots \mathbf{b}^{q-1})$ is:

$$\sum_{\ell} \Gamma_\ell \cdot a_\ell \cdot b^1_\ell \cdot b^2_\ell \cdots b^{q-1}_\ell$$

where $a_\ell, b^i_\ell \in \{-1, +1\}$. This is a sum over rounds, and at each round the contribution depends on the **product** $a_\ell \prod_i b^i_\ell$.

### Factoring over rounds

Since the cosine is $\cos(\sum_\ell x_\ell)$ where $x_\ell = \Gamma_\ell a_\ell \prod_i b^i_\ell$, this does NOT factorise over rounds. The exponential form $e^{i \sum_\ell x_\ell} = \prod_\ell e^{ix_\ell}$ does factorise, but the real part (cosine) mixes all rounds through cross-terms.

### Factoring over child branches

The product $\prod_i f(\mathbf{b}^i) H_D^{(m-1)}(\mathbf{b}^i)$ DOES factorise over branches. And if the cosine could be expressed in terms of per-branch quantities, the sum would factorise too.

But $\prod_i b^i_\ell$ couples all branches at each round. This is the fundamental obstruction.

### Parity decomposition trick

However, $\prod_{i=1}^{q-1} b^i_\ell$ only depends on the **parity** of the $b^i_\ell$ values. Define:

$$s_\ell = b^1_\ell \cdot b^2_\ell \cdots b^{q-1}_\ell \in \{-1, +1\}$$

There are $2^{2p+1}$ possible parity vectors $\mathbf{s}$. For each fixed $\mathbf{s}$, the number of $(\mathbf{b}^1, \ldots, \mathbf{b}^{q-1})$ configurations that produce that parity can be computed from the individual branch tensors.

Specifically, define the **parity-projected branch tensor**:

$$\tilde{H}^{(m)}_\pm(\ell) = \sum_{\mathbf{b}: b_\ell = \pm 1} f(\mathbf{b}) H_D^{(m-1)}(\mathbf{b})$$

Then the number of (q-1)-tuples with $s_\ell = +1$ at position $\ell$ involves $q-1$ copies having an even number of $-1$'s at that position... This gets combinatorially complex.

## Assessment: Where This Leaves Us

### What's feasible

1. **Large-D iteration** ($O(p^2 \cdot 4^p)$): straightforward to implement, gives $O(1/D)$-approximate results. At $D = 4$ the error is ~25%, which is significant but could provide useful bounds and starting points for angle optimisation.

2. **Exact brute-force** ($O(2^n)$ where $n \sim 6^p$): what we already have. Capped at p=1 for (k=3, D=4) — p=2 requires 129 qubits (3 + 18 + 108), far beyond the 22-qubit guard. For k=2, D=3: feasible up to p≈4 (30 qubits).

3. **Exact finite-D Basso iteration** ($O(p \cdot 4^{3p})$): Basso Eq. (8.7) directly. For k=3:
   - p=5: $4^{15} \approx 10^9$ — feasible (minutes)
   - p=7: $4^{21} \approx 4 \times 10^{12}$ — hard (cluster, hours)
   - p=10: $4^{30} \approx 10^{18}$ — infeasible

4. **Hybrid approach**: use the exact finite-D iteration at moderate p, then extrapolate or use the large-D iteration for higher p.

### What's NOT feasible

The original P1.3 spec's claim of $O(4^p)$ cost at any k — this only holds for k=2. For k=3 the cost is at minimum $O(4^{3p})$ at finite D, or $O(4^p)$ only in the large-D limit.

### Recommendations

1. **Implement the exact finite-D Basso iteration** ($O(4^{3p})$ for k=3). This gives exact results and is feasible up to p ≈ 5–7 depending on compute budget. That may be enough to see whether QAOA is competitive with SA.

2. **Also implement the large-D iteration** ($O(4^p)$) as a comparison point. The $O(1/D)$ error at $D=4$ needs to be quantified empirically — compare against exact results at the p values where both are feasible.

3. **Look for a parity decomposition** that reduces $O(4^{3p})$ to something like $O(4^{2p})$. The parity structure of the k-body gate should allow partial factoring. This is a research problem worth investigating but not blocking.

4. **Update the P1.3 spec** to reflect the true cost and implement the Basso Eq. (8.7) iteration directly.

---

## Worked Example: k=3, D=4, p=1

At p=1, the hyperindex has 2p+1 = 3 components (in Basso's convention) or 2 bits (in our convention). Let me verify against the brute-force simulator.

The brute-force simulator gives:
- `qaoa_expectation(TreeParams(3, 4, 1), QAOAAngles([0.0], [0.0])) = 0.5` (zero-angle baseline)
- `parity_expectation(TreeParams(3, 4, 1), QAOAAngles([0.0], [0.0])) = 0.0`

At non-zero angles (e.g. γ=0.3, β=0.5):
- These can be computed by the brute-force simulator and should match the Basso finite-D formula

**TODO for developer agent**: Implement Eq. (8.7) and verify numerical agreement with the brute-force simulator at p=1 for `(k=3, D=4)`, plus the overlapping MaxCut cases where Tier 1 remains feasible.

---

## Summary

| Approach | Cost | Exactness | Feasible p (k=3,D=4) |
|----------|------|-----------|----------------------|
| Brute-force state vector | $O(2^{6^p})$ | Exact | p=1 only (p=2 needs 129 qubits) |
| Basso finite-D iteration (Eq. 8.7) | $O(p \cdot 4^{3p})$ | Exact | p ≤ 5–7 |
| Basso large-D iteration (Eq. 8.9) | $O(p^2 \cdot 4^p)$ | $O(1/D)$ approximate | p ≤ 15+ |
| Original P1.3 spec (wrong) | $O(p \cdot 4^p)$ | N/A | N/A |
